#include <program1.h>
plantilla(Tipo)
procedimiento muestVec(entero t,Tipo v[]) {
entero ind;
variar(ind,0,t-1,1)
    mostrar << v[ind] << tabulado;
    finVariar
mostrar << salto;
}

implementa procedimiento muestVec(entero,real[]);
//implementa procedimiento muestVec(entero,entero[]);
//implementa procedimiento muestVec(entero,carater[]);