#include <program1.h>                                           // Archivo de traducción de seudocódigo a C++

procedimiento leeVec(entero tam, real v[]) {
entero subI;
variar(subI,0,tam-1,1)
    leerM(v[subI],"Valor("<<subI+1<<"):");
    finVariar
}
procedimiento muestVec(entero tam, real v[]) {
entero subI;
variar(subI,0,tam-1,1)
    mostrar << v[subI] << tabulado;
    finVariar
mostrar << salto;
}

procedimiento leeVec(entero tam, entero v[]) {
entero subI;
variar(subI,0,tam-1,1)
    leerM(v[subI],"Valor("<<subI+1<<"):");
    finVariar
}
procedimiento muestVec(entero tam, entero v[]) {
entero subI;
variar(subI,0,tam-1,1)
    mostrar << v[subI] << tabulado;
    finVariar
mostrar << salto;
}
procedimiento leeVec(entero tam, doble v[]) {
entero subI;
variar(subI,0,tam-1,1)
    leerM(v[subI],"Valor("<<subI+1<<"):");
    finVariar
}
procedimiento muestVec(entero tam, doble v[]) {
entero subI;
variar(subI,0,tam-1,1)
    mostrar << v[subI] << tabulado;
    finVariar
mostrar << salto;
}
