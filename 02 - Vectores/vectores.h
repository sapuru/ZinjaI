procedimiento leeVec(entero,real[]);
procedimiento muestVec(entero,real[]);
procedimiento leeVec(entero,entero[]);
procedimiento leeVec(entero,doble[]);
procedimiento muestVec(entero,entero[]);
procedimiento muestVec(entero,doble[]);
